#include "Map.h"

Map::Map(char const *tiles): tiles(tiles) {}

void Map::regenPath() {
    int cursor = 0;
    for (int i = 0; tiles[i]; i++) {
        // TODO: I will do this shit later.
    }
    // TODO: add the dudes into the mix as well.
}